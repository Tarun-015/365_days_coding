CPU Scheduling is the process by which the operating system decides which process gets the CPU next —
when there are multiple ready processes waiting.


FCFS (First Come First Serve)		

SJF (Shortest Job First)	

LJF(Longest Job First)
