Key Concepts
- `Class.forName()` → Loads class dynamically.
- `getDeclaredFields()`, `getDeclaredMethods()`, `getConstructors()` → Fetch metadata.
- `setAccessible(true)` → Bypass private access.
- `invoke()` → Call a method at runtime.
